name: Review Request Test
on:
  push:
  pull_request:
    types: [review_requested]
    branches:
      - "*"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:

      - name: 🔔 Notify Review
        uses: ./
        with:
          channel-id: 'guides'
